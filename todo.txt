[AKTUELLE BAUSTELLEN]

abrechnung und auswertung richtig? man wählt august aus aber kriegt juli + august (august bis 9.)

alle ajax.fail() mit json parse?

KEHLER ABRECHNUNG
    -> Sonstiges? abmelden ... -> benötigt?
    -> Status? 450, student ... -> benötigt?
    -> Unterschrift benötigt?

JONAS
    -> abrechnung: unterschrift? reicht kennung / personalnummer / passwort?

DATENBANK
    -> vor und nachname trennen (nur aushilfe?)
        -> autocomplete array dann beides zusammenfügen (in getdata?)

- ABRECHNUNG
    -> urlaubstage in abrechnung
    -> leere felder für aushilfen ohne zeiten diesen monat?
    -> feld für besonderes? zb. abmelden -> KEHLER
        -> felder in der tabelle bearbeiten können?
    -> sortiertung? personalnr oder name? -> eher name
    -> abrechnung nur wenn alle personalnummern da sind / einzelne ohne nicht aufführen mit warnung

- EINZELAUSWERTUNG
    -> name auswerten ändern
    -> css cols besser machen
        -> tabelle zu weit rechts
    -> doppelter Eintrag hat einen Tag zu viel

- ZEITEN
    -> für admin ist alles einsehbar
        -> für den rest nur eigene station
        -> filterbar (kriterien?)

- AUSHILFEN
    -> name und pn nicht veränderbar
    -> info bei anlegen
        -> wenn pn nicht vorhanden -> leer lassen
        -> personalnummer ein mal veränderbar und dann gesperrt (bei null?)
            -> bei eintragen check ob unique
    -> Bei anlegen -> lohn wochentag, lohn samstag, lohn sonntag... (placeholder geht nich)
    -> punkt statt komma (string replace)
        -> punkt statt komma aus cation raus
    -> bei keiner pn -> exisitiert bereits (wegen 0)
    -> nach anlegen auf zurück -> page reload statt show

- ADMIN
    -> PN und name der aushilfe verändern

[BFI]
- eintragen -> gehalt, toFixed(2) rundet falsch (bei 78,115), mysql auch. ungerundet in tabelle oder geht runden?
- MS ACCESS zusätzlich zusätzlich zu phpMyAdmin?
- Machen die Tabellen in Abrechnung / Auswerten / Aushilfen so Sinn (vom erstellen per loop usw)?
- main.js .get() ist komisch
- eaget.php & getdata.php 1 Query?
- abrechnung zeitraum 10.x.x - 9.x.x optimieren? abget und eaget
- wie gehe ich am besten mit sql fehlermeldungen um?

[WICHTIG]
- expire auf 3 min
- emulate an oder aus

[SIMGE, ANNE]
- Urlaub fertig (Anne, Simge?)
    -> alle 13 wochen? 3 monate?
    -> schon 6 monate sonst bla
- Nachtragen geht, da jährlich das limit 5400 ist nicht monatlich 450
    -> wie realisieren?

- https://stackoverflow.com/a/8840315/11918503
    -> ajax in funktion wrappen

- seitentitel ändern

- Klick auf autocomplete -> geht nicht weg?

- ALLGEMEIN ajax response in text auf seite? alert? modal?

- gucken wie fehlermeldungen behandelt werden

- Jahresübersicht einbauen für 5400 limit
    -> einzelne aush. / alle?
    -> mit kalender?
    -> von wann bis wann?

- Abrechnung jährlich? Quartal?

- input weite mit css (width) statt col
    -> css flexbox?

- Handbuch / Manual schreiben
    -> Schritt für Schritt
    -> nicht IE benutzen
        -> css geht evtl auch nicht

- code aufräumen
    -> ; hinter } weg
        -> http://inimino.org/~inimino/blog/javascript_semicolons

- footer ordentlich
    -> auch für mobile
    -> was in den footer?
        -> status & station?
    -> css flexbox?

- alle inputs iwie mittig? (besonders aushilfe anlegen)

- alles mit flexbox?
    -> https://css-tricks.com/snippets/css/a-guide-to-flexbox/

- titel ändern einzel abrechnung für pdf
    -> dynamische variable per php?
    
- Bei eintragen, update usw erfolgsecho, sonst fehlermeldung wiedergeben (wie behandeln?)
