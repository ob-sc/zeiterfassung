bohrs
- backup db
- API mit symfony
- datei hochladen

title raus
  -> hwpdata
  -> hbs template
  -> wo sonst?

  BUG
- AH selecten -> beginn ende steht fehler
  -> knopf ist da aber ohne beschriftung
  -> student


tools für sl / gbl
- gbl
  -> zeiten (tabelle zum suchen, (bearbeiten?) & löschen, alle stationen, region?)
    -> hover auf namen in tabelle?
  -> daneben eintragen alt (nachtragen)

- sl
  -> übersicht wer darf noch wie viel (+anzahl schichten?)


- WICHTIG GBL LÖSCHEN UND NACHTRAGEN (BEARBEITEN?)
    -> gbl kann in stationsanmeldung datum bearbeiten?
    -> kann in zeiten auf eine zeit klicken, in modal bearbeiten oder löschen?

- zeiten:
  -> bei sl
    -> knopf zeit löschen an rl melden (auch x?)
    -> der kann an / abnehmen
    -> rl kann auch direkt aus zeiten löschen mit x




- Eintragen neu:
    -> bei klick auf namen direkt id in db
    -> https://tarekraafat.github.io/autoComplete.js/#/

    -> eintrag über ahid nicht name
    -> name kommt bei signedin und eintragen aus aushilfen tabelle
    -> alledaten mit id statt name im index?

    -> Wenn jahrmax überschritten ist geht klasse disabled auf confirm?
        -> Auch bei negativer Arbeitszeit?

    -> Confirm erscheint erst wenn max berechnet wurde?

    -> Schrift bei max Rot

    -> umbenennen auf zeiterfassung?



- loop through object
https://stackoverflow.com/a/41550077/11918503