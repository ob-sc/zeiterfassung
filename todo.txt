[AKTUELLE BAUSTELLEN]

funktion runden einbauen -> auch für bug einzelauswertung? überall bei toFixed(2)?

letzten beide zeiten jim weg -> für bug zeigen lassen

version 1.0 ohne admin

bei jedem prepare direkt $_POST['x']

-gucken ob es noch mehr fehler/erfolgsmeldungen gibt
-erfolgalert wenn erfolg dann echo <script> $('#erfolgAlert').addClass(alert-success) sonst alert danger oder so
-beide alerts direkt mit <h4> (und mb-0?)

aushilfe anlegen status dazu

aushilfe anlegen kann doppelt eingetragen werden, doppelte werden aber nur einmal in der tabelle aushilfe in html angezeit.... insert ignore funktioniert nicht

Datenbank vorbereiten

readme aktualisieren

[BUGS]
- einzelauswertung "schon x über 450" wird zwar gerundet ist aber nicht toFixed(2) sonder nur eine nachkommastelle (jim-roy im august)
- klick auf autocomplete geht nicht weg

- ABRECHNUNG
    -> sortiertung? personalnr oder name? -> eher name
    -> abrechnung nur wenn alle personalnummern da sind / einzelne ohne nicht aufführen mit warnung
    -> automatisch pdf

- EINZELAUSWERTUNG
    -> name auswerten ändern

- AUSHILFEN
    -> info bei anlegen
        -> wenn pn nicht vorhanden -> leer lassen
        -> personalnummer ein mal veränderbar und dann gesperrt (bei null?)
            -> bei eintragen check ob unique
    -> bei keiner pn -> exisitiert bereits (wegen 0)
    -> aushilfe anlegen 2 container -> ajax statt alert?

- ADMIN
    -> tool "wer hat die zeit eingetragen"
        -> datum + aushilfe (?) eingeben
        -> in der zeile id vom disponenten raussuchen
        -> den mit tabelle disponenten vergleichen
    -> tool aushilfe name und / oder personalnummer ändern
    -> station verwaltung: alle aushilfen und disponenten
        -> verwaltung auf alle ändern?

[BFI]
- ?> auch bei include header und bla weg?
    -> auch footer?
- sortierung abget (macht nicht nach nachname -> wegen späterem sql?)
- eintragen -> gehalt, toFixed(2) rundet falsch (bei 78,115), mysql auch. ungerundet in tabelle oder geht runden?
    -> bug einzelauswertung ähnlich

[WICHTIG]
- expire auf 3 min


- seitentitel ändern
    -> header mit php dann jede seite dynamisch

- Jahresübersicht einbauen für 5400 limit
    -> einzelne aush. / alle?
    -> mit kalender?
    -> von wann bis wann?
    -> einfach in einzelauswertung: text so viel dieses jahr -> du musst x weniger oder darfst x mehr
        -> dann text bis 450 -> diesen monat x bis 450
