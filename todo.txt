[AKTUELLE BAUSTELLEN]

WICHTIG: Melly und Anne: Name aus Tabelle zeiten?
    -> name aus senden ajax raus
    -> name aus send.php raus
    -> name aus zeiten tabelle raus

[name]
auch namen disponenten aufdröseln?

überall ajax data zu daten direkt


abrechnung
aushilfen
    vor und nachname bei anlegen


alert statt .text() in eintragen? allgemein?



name = $('#nameInput').val();
id = ahDaten[name].id;

mit id aus Datenbank





Datenbank vorbereiten

tage ganz ausschreiben

readme aktualisieren

kehler urlaub auf halbe / ganze tage

DATENBANK
    -> vor und nachname trennen (nur aushilfe?)
        -> autocomplete array dann beides zusammenfügen (in getdata?)

- ABRECHNUNG
    -> urlaubstage in abrechnung
    -> leere felder für aushilfen ohne zeiten diesen monat? WICHTIG für zb abmelden
    -> sortiertung? personalnr oder name? -> eher name
    -> abrechnung nur wenn alle personalnummern da sind / einzelne ohne nicht aufführen mit warnung
    -> automatisch pdf

- EINZELAUSWERTUNG
    -> name auswerten ändern

- AUSHILFEN
    -> info bei anlegen
        -> wenn pn nicht vorhanden -> leer lassen
        -> personalnummer ein mal veränderbar und dann gesperrt (bei null?)
            -> bei eintragen check ob unique
    -> bei keiner pn -> exisitiert bereits (wegen 0)
    -> aushilfe anlegen 2 container -> ajax statt alert

- ADMIN
    -> tool "wer hat die zeit eingetragen"
        -> datum + aushilfe (?) eingeben
        -> in der zeile id vom disponenten raussuchen
        -> den mit tabelle disponenten vergleichen

[BFI]
- WICHTIG urlaub todo in gleiches query? muss 1 array durchlaufen
- in abget.php (abrechnung) auch aushilfen auflisten, die nicht gearbeitet haben
- eintragen -> gehalt, toFixed(2) rundet falsch (bei 78,115), mysql auch. ungerundet in tabelle oder geht runden?
- MS ACCESS zusätzlich zusätzlich zu phpMyAdmin?
- Machen die Tabellen in Abrechnung / Auswerten / Aushilfen so Sinn (vom erstellen per loop usw)?
- main.js .get() ist komisch
- eaget.php & getdata.php 1 Query?
- abrechnung zeitraum 10.x.x - 9.x.x optimieren? abget und eaget
- wie gehe ich am besten mit sql fehlermeldungen um?
- vor und nachname trennen?
    -> Was ist mit dem Input (anlegen, eintragen usw)?

[WICHTIG]
- expire auf 3 min


- seitentitel ändern
    -> header mit php dann jede seite dynamisch

- Klick auf autocomplete -> geht nicht weg?

- Jahresübersicht einbauen für 5400 limit
    -> einzelne aush. / alle?
    -> mit kalender?
    -> von wann bis wann?

- Abrechnung jährlich? Quartal?

- titel ändern einzel abrechnung für pdf
    -> dynamische variable per php?
