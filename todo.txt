    _________________
    |               |
    |    ISSUES     |
    |_______________|

- Eintragen neu:
    -> bei klick auf namen direkt id in db
    -> https://tarekraafat.github.io/autoComplete.js/#/

    -> eintrag über ahid nicht name
    -> name kommt bei signedin und eintragen aus aushilfen tabelle
    -> alledaten mit id statt name im index?

    -> statt 

GESPRÄCH CK MH JO:
- Nicht mehr über 450
    -> Rückwirkend mehr wieder rein
- Nur noch Namen (nicht mehr billstedt sondern rothehueser)
- Pausenzeiten müssen rein
- Über 10 Stunden stop
- morgens eintragen abends abmelden
    -> keine zeiten nachtragen
- fehlermeldung bei über durchschnitt

- manual überarbeiten

- löschen ordentlich rausnehmen

- Status
    -> Bei anlegen: nicht input text sondern input number mit step 1
    -> form serialize geht wohl nicht mehr sondern ein objkt mit dem jeweils richtigem Status
    -> bei status bearbeiten nimmt er nur noch integer

- Durchschnitt
    -> aus funktion rausnehmen
    -> emedian.php raus
    -> auswerten (und eintragen)
        -> gucken welche datenobjekte & variablen nicht mehr gebraucht werden

- andere lösung für aus anderer Station auf abrechnung für lohnbüro

- nachträgliche bearbeitung (eintragen & löschen) von zeiten im vormonat (& alle?) sperren

- personalnummer nicht selber eintragen
    -> soll das lohnbüro das machen?

- api: 
    -> wenn monatsabrechnung runtergeladen wurde:
        -> stiller eintrag in db
        -> runtergeladen mit regdate und von wem
    -> historie wer sich wann anmeldet?
        -> für controlling gbl

- bei monatsberechnung (durchschnitt?) schaut er nicht in welchen monat eingetragen wird

- bei abrechnung undefined rausnehmen WICHTIG
    -> passiert wenn aushilfe gelöscht wurde aber zeit noch bei in zeiten gefunden wurde
   
- bei eintragen:
    -> schon angemeldet heute (bei anmelden)
    -> schon zeit heute (bei eintragen)
        -> warnung/fehler wenn gleiche arbeitszeit eingetragen wird (zb 1.1.2019 13 uhr bis 15 uhr und 1.1.2019 14 uhr bis 15 uhr)
            -> in vorschau oder fehler?

- wenn über jahresdurchschnitt
    -> kein eintragen möglich

- alles jquery zu vanilla js

- jquery ajax zu fetch()

    ________________
    |              |
    |     TODO     |
    |______________|

- wirklich löschen ein modal in header handlebar dings?
    -> zu info und alert

- bei zb abrechnung
    -> automatisch ok mit vorgegebenem monat

- aushilfe anlegen nicht übereinander sonder auch nebeneinander
    -> allg gucken wo forms schöner könnten

-> allg gucken obs probleme beim jahreswechsel gibt?

- favicon als ico
    -> neu machen?

- kein window.location.reload mehr
    -> entweder dom element löschen oder ajax neu

- filter clear zeiten

- aushilfen: 
    -> fehler wenn es eine aushilfen schon gibt?
    -> ein array -> namen pushen -> nächster name -> gucken obs schon gibt -> pushen oder halt fehler

- bei aushilfen (& andere) seite nicht neu laden sondern wie bei mitarbeiter we eintragen aktualisieren

- knöpfe gleiche größe
    -> besonders aushilfen löschen modal

- favicon ändern?

- responsive machen
    -> navbar nicht scalen sondern mit @media min-width wie eintragen

- trim (nur bei aushilfe anlegen?)

- eaget und eatabelle umschreiben? zeiten (tage) und summen in ein array? mit while($row = $stmt-fetch...) wie in abget?

- in aushilfen anlegen -> alleDaten statt ahDaten[name] um zu gucken ob es sie insgesamt schon gibt

- jeweils element zu key? -> https://stackoverflow.com/a/41550077/11918503

- urlaubsberechnung richtig machen (es funktioniert nicht, wenn man im abrechnungszeitraum januar ist der im vorjahr startet, erst ab dem 1.1. wieder)

- statt namen immer id eintragen
    -> alle daten als index id nicht name
    -> aus db bei eintragen und zeiten (u.a.) kommt id => name suchen

- css in js direkt importieren statt cssbundle?
    -> also eintragen.css in eintragen.js

- loop through object
https://stackoverflow.com/a/41550077/11918503