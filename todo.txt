[AKTUELLE BAUSTELLEN]
titel ändern einzel abrechnung für pdf
aus allen tabellen scope raus

- ABRECHNUNG
    -> urlaubstage in abrechnung
    -> Abrechnungen vom 10.-9. WICHTIG
    -> feld für besonderes? zb. abmelden
        -> felder in der tabelle bearbeiten können?
    -> druckansicht

    -> station in getdata und auslesen für überschrift?

- EINZELAUSWERTUNG
    -> name auswerten ändern
    -> auch station filtern?
    -> css cols besser machen
        -> tabelle zu weit rechts

- BEARBEITEN ->  AUSHILFEN
    -> nur admin + sl
    -> knopf fürs anlegen
        -> bei anlegen aushilfe direkt session station auslesen und einfügen
    -> nur der gespeicherte Eintrag wird gespeichert
    

- ACCOUNTS
    -> station (zb. osdorf)
    -> sl (zb. sl_osdorf)   
        -> abrechnung monat nur sl/idl
        -> übersicht alle aushilfen und gehälter
            -> verifizierung durch personalkürzel, nr, was auch immer
        -> aushilfen anlegen / bearbeiten
    -> admin
        -> select für stationen
        -> user anlegen / hash
        -> sql für zb zeiten.php if ($station == admin) { $sql = ... }

    -> jeweils einen status in benutzer
    -> dann Menüpunkte entweder .disabled oder php if($status == admin){echo "<a href...";}
        -> extra include für menü?
        -> per json getdata auslesen welcher status (getdata) -> dementsprechend menü (mit .disabled?)

- ZEITEN
    -> für admin ist alles einsehbar
        -> für den rest nur eigene station
        -> filterbar (kriterien?)

- TODO erledigen

- Passwort für Stationen + mitarbeiterkennungen
    -> erstmal selber vergeben -> ändern lassen?

[TABELLEN]
- alles abstrakt halten
    -> wenn etwas dargestellt werden soll dann über javascript
- über id suchen, nicht :name und so weiter

SELECT s.name, az.lohn
FROM arbeitszeit as az
JOIN station AS s ON s.id=az.station
JOIN aushilfen as ah ON ah.id=az.name
WHERE az.datum = '14.08.2019'

[BFI]
- MS ACCESS zusätzlich zusätzlich zu phpMyAdmin?
- Machen die Tabellen in Abrechnung / Auswerten / Aushilfen so Sinn (vom erstellen per loop usw)?

[WICHTIG]
- expire auf 3 min
- emulate an oder aus

[SIMGE, ANNE]
- Urlaub fertig (Anne, Simge?)
    -> alle 13 wochen? 3 monate?
    -> schon 6 monate sonst bla
- Nachtragen geht, da jährlich das limit 5400 ist nicht monatlich 450
    -> wie realisieren?

- ids in tabellen eintragen statt namen (station, disponenten usw)

- https://stackoverflow.com/a/8840315/11918503
    -> ajax in funktion wrappen

- seitentitel ändern

- gucken wie fehlermeldungen behandelt werden

- Jahresübersicht einbauen für 5400 limit
    -> einzelne aush. / alle?
    -> mit kalender?
    -> von wann bis wann?

- reset autoincrement id
    -> auf 6k, warum auch immer

- Abrechnung jährlich? Quartal?

- input weite mit css (width) statt col

- Handbuch / Manual schreiben
    -> Schritt für Schritt
    -> nicht IE benutzen
        -> css geht evtl auch nicht

- mehr kommentare damit ich den code später noch check

- ; hinter } weg
    -> http://inimino.org/~inimino/blog/javascript_semicolons

- footer ordentlich
    -> auch für mobile
    -> was in den footer?

- reset knopf bei eintragen?

- tabelle für eintrag-vorschau? (vor senden)

- Pause umgehen? wird nicht gemacht, dann wird unnötig zeit abgezogen
    -> entsprechende arbeitszeit hinten addieren dann abziehen

- input"month" kein support safari firefox
    -> input"date" kein safari: placeholder="dd-mm-yyyy"
    -> https://caniuse.com/#feat=input-datetime

- abrechnung nur wenn alle personalnummern da sind / einzelne ohne nicht aufführen mit warnung

- Seite wie aushilfen (bearbeiten) für mitarbeiterkennungen?
    -> nur SL
